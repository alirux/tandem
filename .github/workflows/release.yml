name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      # -------------------------------------------------------------------------
      # Maven Central publishing — DISABLED for now (phase 2, not yet set up).
      #
      # To re-enable: uncomment the steps below, register the `com.codingful`
      # namespace on the Central Portal, publish a GPG key, and add the repo
      # secrets. NOTE: before re-enabling, verify the env-var names actually match
      # what the com.vanniktech.maven.publish plugin reads (it typically expects
      # ORG_GRADLE_PROJECT_mavenCentralUsername / ...Password and
      # ORG_GRADLE_PROJECT_signingInMemoryKey / ...Password, not the names below).
      #
      # - uses: actions/setup-java@v5
      #   with:
      #     distribution: temurin
      #     java-version: 17
      #
      # - name: Setup Gradle
      #   uses: gradle/actions/setup-gradle@v5
      #
      # - name: Extract version from tag
      #   id: version
      #   run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
      #
      # - name: Publish to Maven Central
      #   run: ./gradlew publishToMavenCentral
      #   env:
      #     VERSION: ${{ steps.version.outputs.VERSION }}
      #     MAVEN_CENTRAL_USERNAME: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
      #     MAVEN_CENTRAL_PASSWORD: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}
      #     GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
      #     GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
      # -------------------------------------------------------------------------

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          name: ${{ github.ref_name }}
          generate_release_notes: true
